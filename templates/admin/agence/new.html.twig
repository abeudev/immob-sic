{% extends 'admin/layout/base.html.twig' %}

{% block title %}
	{{ 'Ajouter une agence' | trans }}
{% endblock %}

{% block body %}

	<ul class="nav nav-tabs">
		<li class="nav-item">
			<a class="nav-link" href="{{ path('admin_agences') }}">
				<i class="fas fa-gavel text-danger"></i>
				{{ 'agences'|trans }}
			</a>
		</li>
		<li class="nav-item active">
			<span class="nav-link active">
				<i class="fas fa-plus-square text-danger"></i>
				{{ 'créer une agence'|trans }}
			</span>
		</li>
	</ul>

	<div class="card mb-3">

		<div class="card-body">

			{{ form_start(form) }}

			<div class="form-group" id="structure_id">
				{{ form_row(form.structure_id) }}
			</div>

			<div class="form-group" id="libelle">
				{{ form_row(form.libelle) }}
			</div>

			<div class="form-group" id="email">
				{{ form_row(form.email) }}
			</div>

			<div class="form-group" id="contact">
				{{ form_row(form.contact) }}
			</div>

			<div class="form-group" id="email">
				{{ form_row(form.adresse) }}
			</div>


			<div class="form-group">

				<button type="submit" class="btn btn-primary">
					{{ 'Créer une agence'|trans }}
				</button>

				{{ form_widget(form.saveAndCreateNew, {label: 'Enregistrer et créer une nouvelle agence', attr: {class: 'btn btn-primary'}}) }}

			</div>

			{{ form_end(form) }}

		</div>
	</div><br>

{% endblock %}

{% block javascripts %}

	{{ encore_entry_script_tags('js/slugger') }}

{% endblock %}